export const sortResultsPanelStyles = theme => ({
    paperWrapper: {
        marginBottom: theme.s,
        padding: theme.spacing.unit * 3
    },
    marginRight: {
        marginRight: theme.spacing.unit * 2
    },
    marginBottom: {
        marginBottom: theme.spacing.unit * 3
    }
});
